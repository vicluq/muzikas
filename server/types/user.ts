export interface User {
    name: string;
    email: string;
    password: string;
    tokenExpiration: number;
    token: string;
}

export interface Suppiler {
    name: string;
    email: string;
    password: string;
    tokenExpiration: number;
    token: string;
}